# Chroma Vector Database

This folder stores the persistent ChromaDB database.

It will automatically populate with:
- Embeddings (Parquet files)
- Document chunks
- Metadata (page numbers, filenames)
- SQLite index files
- UUID mappings

